node_modules/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example

# Android build outputs and caches
.gradle/
android/.gradle/
android/app/build/
android/build/
android/captures/
*.apk
*.aab
*.jar
*.bin

# Llaves VAPID generadas automaticamente (contienen llave privada)
vapid_keys.json

# Archivos de datos que el servidor escribe en runtime (no son codigo)
invoices_permanent_backup.json
payments_permanent_backup.json
clients_local.json
notifications_local.json
offers_extra.json
push_subscriptions.json
excluded_critical.json
scripts/git-sync.cjs
